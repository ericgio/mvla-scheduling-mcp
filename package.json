{
  "name": "mvla-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "node scripts/version-docs.js && yarn workspace mvla-client build && yarn workspace mvla-scheduling-mcp build",
    "build:client": "node scripts/version-docs.js && yarn workspace mvla-client build",
    "build:server": "yarn workspace mvla-scheduling-mcp build",
    "dev:client": "yarn workspace mvla-client dev",
    "dev:server": "yarn workspace mvla-scheduling-mcp dev",
    "start": "yarn workspace mvla-scheduling-mcp start"
  }
}
